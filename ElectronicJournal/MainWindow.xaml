<Window x:Class="ElectronicJournal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectronicJournal"
        mc:Ignorable="d" Closing="OnWindowClosing" Icon="/Resources/icon.png"
        Title="Электронный журнал" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden" Grid.Column="0" Grid.Row="1"/>

        <TextBlock Text="{Binding Path=Time,StringFormat='dd.MM.yyyy HH:mm:ss'}" Grid.Column="0"
                   Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0 0 15 15" Name="Time"/>

        <Button Content="Назад" Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left"
                Margin="10 10 0 0" Width="50" Height="25" Name="BackButton" Click="OnBackButtonClick"/>
    </Grid>
</Window>
